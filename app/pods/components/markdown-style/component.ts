import Component from '@glimmer/component';

interface MarkdownStyleArgs {
    indexRouteColor: string;
    indexRouteDisplay: string;
}

export default class MarkdownStyle extends Component<MarkdownStyleArgs> {}
